{% if items|length %}
	<ul class="{{ name }} {{items|length == 1 ? 'single-nav-menu' : ''}}">
		{% for item in items %}
			<li>
				<a class="{{ item.classes|join(' ') }} menu-link" target="{{ item.target }}" href="{{ item.link }}">{{ item.title }} </a>
			</li>
		{% endfor %}
	</ul>
{% endif %}