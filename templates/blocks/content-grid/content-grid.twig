{#
  Title: Content Grid
  Description: Content Grid
  Category: triumph-blocks
  Icon: grid-view
  Keywords: 
  Mode: edit
  Align: left
  PostTypes: post page
  SupportsAlign: false
  SupportsMode: false
  SupportsMultiple: true
#}

{% embed 'blocks/layout-wrapper/layout-wrapper.twig' with {blockName: "content-grid" } %}
	{% block block_content %}
		<ul class="content-grid-list">
		{% for grid_item in fields.grid_items %}
			<li class="content-grid-item grid-item-{{ fields.grid_item_style }}">
				{% if grid_item.image %}
					<div class="grid-item-header image">
						{% set image = Image(grid_item.image) %}
						<img src="{{ image.src }}" srcset="{{ image.srcset }}" data-object-fit="cover"/>
					</div>
				{% endif %}

				{% if grid_item.copy %}
					<div class="grid-item-copy">
						{{ grid_item.copy }}
					</div>
				{% endif %}

				{% if grid_item.link.link.url  %}
					<div class="grid-item-footer">
						<a class="btn {{ grid_item.link.link_style }} {{ grid_item.link.link_color }}" href="{{ grid_item.link.link.url }}">{{ grid_item.link.link.title }}</a>
					</div>
				{% endif %}
			</li>
		{% endfor %}
		</ul>
    {% endblock %}
{% endembed %}
